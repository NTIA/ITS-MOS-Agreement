[project]
name = "mos_agreement"
version = "0.1.0"
description = ""
authors = [
    {name = "jpieper@ntia.gov"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
